
def model_fun():
    # import pickle
    # import numpy as np
    # model = pickle.load(open('model.pkl', 'rb'))
    # features = 'hanzala'

    # prediction = np.argmax(model.predict(features), axis=-1)

    return 5